{
  "scripts": {
    "deploy-all:dev": "sls deploy -s dev",
    "deploy-all:prod": "sls deploy -s prod",
    "deploy:dev": "sls deploy -f abs -s dev",
    "deploy:prod": "sls deploy -f abs -s prod",
    "doctor": "sls doctor"
  },
  "devDependencies": {
    "serverless": "^3.19.0"
  }
}
