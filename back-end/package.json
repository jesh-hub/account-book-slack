{
  "scripts": {
    "deploy-all:dev": "sls deploy -s dev --verbose",
    "deploy-all:prod": "sls deploy -s prod --verbose",
    "deploy:dev": "sls deploy -f abs -s dev --verbose",
    "deploy:prod": "sls deploy -f abs -s prod --verbose"
  },
  "devDependencies": {
    "serverless": "^3.19.0"
  }
}
