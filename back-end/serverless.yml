service: abs
frameworkVersion: '3'

useDotenv: true

provider:
  name: aws
  region: ap-northeast-1
  architecture: x86_64
  runtime: go1.x
  memorySize: 128
  timeout: 10
  environment:
    DB_HOST: ${env:DB_HOST}
    DB_AUTH: ${env:DB_AUTH}
    DB_NAME: ${env:DB_NAME}

package:
  exclude:
    - ./**
  include:
    - ./bin/**

functions:
  abs:
    handler: bin/abs
    events:
      - httpApi:
          path: /{proxy+}
          method: any
